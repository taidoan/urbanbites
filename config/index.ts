import { root, source, styles, assets, images } from "./paths";

/**
 * Configuration object containing paths
 */
export const config = {
  rootPath: root,
  sourcePath: source,
  stylesPath: styles,
  assetsPath: assets,
  imagesPath: images,
};
